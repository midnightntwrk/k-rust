name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  rust:
    name: Rust
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
          targets: wasm32-unknown-unknown

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Build native targets
        run: cargo build --workspace --all-targets --locked

      - name: Run Clippy with native Z3 inference
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

      - name: Run tests with native Z3 inference
        run: cargo test --workspace --locked

      - name: Check portable feature set
        run: cargo clippy --workspace --all-targets --no-default-features --locked -- -D warnings

      - name: Test portable feature set
        run: cargo test --workspace --no-default-features --locked

      - name: Check WebAssembly compatibility
        run: cargo build --workspace --target wasm32-unknown-unknown --no-default-features --locked
