case = "let-list-binder"
subsystem = "kompile"
source_ticket = "B1-02"
harvested_from = "wasm-semantics wasm-data/sparse-bytes.k:82-84 at 212271bd434bd402e27c42f6416854342733386d, reduced by hand (stage-12 finding on the wasm haskell/rust semantic comparator)"
k_revision = "4a46d1231473b599c699160132fd6e76a5c46406"
haskell_backend = "0.1.155 (38afc81)"

[[artifact]]
file = "verify.exit"
command = 'set +e; kompile test.k --backend haskell --main-module LET-LIST-BINDER --syntax-module LET-LIST-BINDER --output-definition ref && kore-parser ref/definition.kore >/dev/null; rc=$?; printf "%s\n" "$rc" > verify.exit; exit "$rc"'
tool = "kore-parser"
exit_code = 0
sha256 = "9a271f2a916b0b6ee6cecb2426f0b3206ef074578be55d9bc94f6f3fe3ab86aa"

# Generated `#lambda` productions of ref/definition.kore, keyed by the source line of the
# `#let`/`#fun` expression (the Location attribute of the generated lambda rule).
# `arguments` lists the KORE argument sorts of the symbol declaration; `total` records the
# presence of the `total` attribute. ResolveFun.sort reads the binder pattern after the
# parser's AddEmptyLists completion: at line 12 the bound variable is inferred `Chunk`
# from its user-list element position, the binder is completed to `SUB .Chunks`, and the
# lambda takes `lub(Chunks, Chunks)` with a non-variable (non-total) pattern.

[[lambda]]
line = 12
arguments = ["Chunks", "Chunks", "Int"]
total = false

[[lambda]]
line = 15
arguments = ["Chunks", "Int"]
total = true

[[lambda]]
line = 18
arguments = ["Chunks", "Int"]
total = true

[[lambda]]
line = 21
arguments = ["Chunks", "Int"]
total = true
