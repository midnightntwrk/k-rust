C4-05 issue-2287 predicate-floating oracle fixture.

`a4-spec.k` is the reference claim that K proves as `#Top`. `a4-pattern.kore`
isolates the emitted shape: an equality predicate nested under a constructor.

`a5-spec.k` is the companion reference claim whose simplification equation has
an ML `#Top` RHS. `a5-pattern.kore` isolates the emitted equation shape.
